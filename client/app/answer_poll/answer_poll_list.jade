div(ng-include='"components/navbar/navbar.html"')

.container
  ul(ng-repeat='poll in polls')
    li
      a(href='' ng-click='answerPoll(poll._id)') Answer the poll 
        em "{{ poll.name }}"
.footer
  | Foo bar
